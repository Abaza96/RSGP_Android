<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.justgraduationproject.android.recommenderapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".NotificationsActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MessagingActivity" />
        <activity android:name=".CimmunityActivity" />
        <activity android:name=".CommunityActivity" />
        <activity android:name=".UserProfile" />
        <activity android:name=".BooksSectionActivity" />
        <activity android:name=".FollowersSection" />
        <activity android:name=".ActivitySection" />
        <activity android:name=".BookDetails"></activity>
    </application>

</manifest>